export default function primitiveSample () {
    let name = "1"
    console.log("sample1:", typeof name, name )

    let isSingle: boolean = false
    console.log("sample ", typeof isSingle, isSingle)
    let age: number = 28
    const isvoer28: boolean = age >= 20;
    console.log("sample1", typeof isvoer28, isvoer28)
    

}